%tr{class: ("current_item" if item == @current_item)}
  %td
    = item.quantity
    &times;
  %td= item.product.title
  %td.item_price= number_to_currency(item.total_price)
  %td.remove_item= button_to 'Usuń', item, method: :delete, remote: true 