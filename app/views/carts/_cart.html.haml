%h2 MÓJ KOSZYK
%table
  = render(cart.items)
  %tr.total_line
    %td.total{:colspan => "2"} Total
    %td.total_cell= number_to_currency(cart.total_price)
#remove= button_to 'Opróżnij koszyk', cart, method: :delete, data: {confirm: 'Na pewno?'}