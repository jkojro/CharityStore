- if !@cart.items.empty?
  %h2 MÓJ KOSZYK
  %table
    - @cart.items.each do |item|
      %tr
        %td
          = item.quantity
          ×
        %td= item.product.title
        %td.item_price= number_to_currency(item.total_price)
        %td.remove_item= button_to 'Usuń', item, method: :delete, data: {confirm: 'Jednak oszczędzasz?'} 
    %tr.total_line
      %td.total{:colspan => "2"} Total
      %td.total_cell= number_to_currency(@cart.total_price)

  = button_to 'Opróżnij koszyk', @cart, method: :delete, data: {confirm: 'Na pewno?'}
- else
  %h2 Twój koszyk jest pusty. Czas na zakupy!